<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
</head>

<body>
    <br>
    <div class="container">
        <hr>
        <div class="card"> 
            <div class="card-header">
                <h2>Cadastro de demandas </h2>
            </div>
            <div class="card-body">
                <form method="POST" enctype="multipart/form-data"<% if (demanda.id) { %> action="/update/<%= demanda.id %>" <% }else{ %> action="/save/"   <%} %> >
                    <input class="form-control" type="text" name="nome" placeholder="Digite a demanda..." <% if (demanda) { %> value="<%= demanda.nome %>"><% } %><br>
                    <input class="form-control" type="file" name="file" ><br>
                    <% if(demanda.file){ %>  <a href="/<%=demanda.file %>" Target="_blank">ver upload</a> <a href="/deleteUpload/<%=demanda.id %>"  class="btn btn-danger">Excluir</a><% } %><br><br>
                    <% if (demanda.id) { %> <input type="hidden" name="id" value="<%= demanda.id %>"><% } %>
                    
                    <button class="btn btn-success">Salvar</button>
                </form>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-w76AqPfDkMBDXo30jS1Sgez6pr3x5MlQ1ZAGC+nuZB+EYdgRZgiwxhTBTkF7CXvN"
        crossorigin="anonymous"></script>
</body>

</html>